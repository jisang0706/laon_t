<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.laont">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true">

        <meta-data
            android:name="com.naver.maps.map.CLIENT_ID"
            android:value="${NAVER_CLIENT_ID}" />

        <activity
            android:name=".fragment.board.PGDetailActivity"
            android:exported="false"
            android:theme="@style/BoardTheme" />

        <activity
            android:name=".fragment.board.PGListActivity"
            android:exported="false"
            android:theme="@style/BoardTheme" />
        <activity
            android:name=".fragment.board.AreaUploadActivity"
            android:exported="false"
            android:theme="@style/Theme.LaonT" />
        <activity
            android:name=".fragment.board.AreaDetailActivity"
            android:exported="false"
            android:theme="@style/BoardTheme" />
        <activity
            android:name=".fragment.board.AreaListActivity"
            android:exported="false"
            android:theme="@style/BoardTheme" />
        <activity
            android:name=".fragment.board.NotiListActivity"
            android:exported="false"
            android:theme="@style/BoardTheme" />
        <activity
            android:name=".intro.NicknameJoin"
            android:exported="false"
            android:theme="@style/AppTheme.NoTitleBar" />
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.LaonT" />
        <activity
            android:name=".intro.IntroActivity"
            android:exported="true"
            android:theme="@style/AppTheme.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>